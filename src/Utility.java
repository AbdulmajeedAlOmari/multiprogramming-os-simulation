/***
 * This class will have all global/configurable variables
 */
public class Utility {

    // Time in (ms) per CPU clock
    public static final int TIME = 1;

    // The input file full path
    public static final String INPUT_FILE_PATH = "";

    // The output file full path
    public static final String OUTPUT_FILE_PATH = "";

    // Dubug mode?
    public static final boolean DEBUG_MODE = false;
}
